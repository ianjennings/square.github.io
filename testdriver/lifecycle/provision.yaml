version: 6.0.0
steps:
  - prompt: Launch Chrome browser and navigate to the Square Open Source Portal
    commands:
      - command: exec
        lang: pwsh
        code: |
          # Launch Chrome with the pre-built and deployed Jekyll site
          # TD_WEBSITE is set by the GitHub Actions workflow to the GitHub Pages URL
          Start-Process "C:/Program Files/Google/Chrome/Application/chrome.exe" -ArgumentList "--start-maximized", "${env:TD_WEBSITE}"
          exit
      - command: wait-for-text
        text: "Square"
        timeout: 30000
      - command: wait-for-text
        text: "Open Source"
        timeout: 10000