version: 6.0.0
steps:
  - prompt: Ensure browser is ready and page is interactive
    commands:
      - command: exec
        lang: pwsh
        code: |
          # Give the page a moment to ensure all JavaScript has executed
          Start-Sleep -Seconds 2
          exit
      - command: wait-for-text
        text: "Square"
        timeout: 10000